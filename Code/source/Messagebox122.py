from GraphicBase122 import *

def showWarning(text = ""):
    # what even is the point, I call showMessage, and showMessage call the
    # showMessage in cmu_112_graphics
    # which basically call the MessageBox of tkinter to show the message
    showMessage(text)